
// passes because this isn't included
//const IncludesGulpUtil = require('../IncludesGulpUtil');

describe('Passes', () => {
  it('even though this should pass', () => {
    expect(1).toBe(1);
  });
});
